import { useEffect,useMemo,useState } from "react";
import PageHeader from "../../components/PageHeader";
import StatusBadge from "../../components/StatusBadge";
import { getAdminExceptions } from "../../services/api";
import "./Admin.css";
export default function Exceptions(){const [items,setItems]=useState([]),[status,setStatus]=useState('ALL'),[severity,setSeverity]=useState('ALL');useEffect(()=>{getAdminExceptions().then(setItems)},[]);const filtered=useMemo(()=>items.filter(e=>(status==='ALL'||e.status===status)&&(severity==='ALL'||e.severity===severity)),[items,status,severity]);return <div><PageHeader title="Exceptions" subtitle="Monitor cases where autonomous processing encountered missing, mismatched, or unverifiable information."/><section className="card admin-panel"><div className="admin-controls"><select className="admin-control" value={status} onChange={e=>setStatus(e.target.value)}><option value="ALL">All statuses</option><option>OPEN</option><option>MONITORING</option><option>RESOLVED</option></select><select className="admin-control" value={severity} onChange={e=>setSeverity(e.target.value)}><option value="ALL">All severities</option><option>HIGH</option><option>MEDIUM</option><option>LOW</option></select></div><div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Exception</th><th>Application</th><th>Applicant</th><th>Type</th><th>Description</th><th>Severity</th><th>Created</th><th>Status</th></tr></thead><tbody>{filtered.map(e=><tr key={e.id}><td>{e.id}</td><td>{e.applicationId}</td><td>{e.applicantName}</td><td>{e.type.replaceAll('_',' ')}</td><td>{e.description}</td><td><span className={`severity-${e.severity}`}>{e.severity}</span></td><td>{e.createdAt}</td><td><StatusBadge label={e.status} tone={e.status==='RESOLVED'?'success':e.status==='OPEN'?'danger':'warning'}/></td></tr>)}</tbody></table></div></section><p className="admin-muted" style={{marginTop:'12px'}}>Exceptions provide visibility into autonomous processing issues. Admission decisions are not manually approved or rejected from this screen.</p></div>}
